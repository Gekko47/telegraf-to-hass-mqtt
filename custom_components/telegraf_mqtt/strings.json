{
  "config": {
    "step": {
      "user": {
        "title": "Configure Telegraf MQTT",
        "description": "Choose how to set up the MQTT topic pattern.",
        "data": {
          "setup_mode": "Setup mode"
        }
      },
      "manual_topic": {
        "title": "Configure Telegraf MQTT",
        "description": "Enter the MQTT topic pattern you want to subscribe to. Entities will be auto-detected from traffic under this pattern.",
        "data": {
          "topic_pattern": "MQTT topic pattern",
          "device_name": "Device name",
          "manufacturer": "Manufacturer",
          "model": "Model",
          "sw_version": "Software version"
        }
      },
      "scan_settings": {
        "title": "Discover topics",
        "description": "The integration will listen to the broker for a short window and show you the topic prefixes it saw. You pick which one to subscribe to. Topic discovery only happens during setup -- it does not stay running.",
        "data": {
          "scan_root_topic": "Scan root topic",
          "scan_duration_seconds": "Scan duration (seconds, 5-300)"
        }
      },
      "pick_topics": {
        "title": "Pick a topic to subscribe to",
        "description": "These are the topic prefixes the broker carried during the scan. A Telegraf-shaped one is pre-selected; keep it or pick another, or type a custom pattern. Each entry subscribes to exactly one pattern -- add another entry to cover more topic roots. Topic-level isolation is preserved -- the integration will not silently widen past the prefix you choose.",
        "data": {
          "topic_pattern": "Topic prefix"
        }
      },
      "reconfigure": {
        "title": "Reconfigure Telegraf MQTT",
        "description": "Update the MQTT subscription, device name, or device metadata. The integration will reload to apply changes.",
        "data": {
          "topic_pattern": "MQTT topic pattern",
          "device_name": "Device name",
          "manufacturer": "Manufacturer",
          "model": "Model",
          "sw_version": "Software version"
        }
      },
      "options": {
        "title": "Configure options",
        "data": {
          "exclude_patterns": "Exclude patterns",
          "field_overrides": "Field overrides",
          "category_overrides": "Category overrides",
          "expire_after": "Expire after",
          "enable_cleanup": "Enable automatic cleanup",
          "cleanup_delay": "Cleanup delay (seconds)",
          "delete_delay": "Delete delay (seconds)",
          "min_active_metrics": "Minimum active metrics per device"
        },
        "data_description": {
          "exclude_patterns": "Glob patterns matched against each metric's unique key; matching metrics are hidden and marked unavailable.",
          "category_overrides": "Map of metric unique keys -- or glob patterns like mem_* -- to config, diagnostic, or null. An exact key always wins over a glob; the first matching glob in definition order is used."
        }
      },
      "scan_running": {
        "title": "Discovering topics",
        "description": "Listening to {probe} for {duration} seconds. Newly seen topics will be collected when the scan completes."
      }
    },
    "error": {
      "invalid_topic": "Enter a valid MQTT subscription topic.",
      "invalid_duration": "Enter a duration between 5 and 300 seconds.",
      "no_traffic_on_scan_root": "No traffic was seen on the scan root topic during the scan window. Try a wider scan root (e.g. telegraf/#) or run the scan again when more devices are publishing.",
      "no_topics_selected": "Pick a topic prefix to subscribe to.",
      "required": "This field is required."
    },
    "abort": {
      "already_configured": "This topic pattern is already configured."
    },
    "progress": {
      "scan_running": "Listening to {probe} for {duration} seconds. Newly seen topics will be collected when the scan completes."
    }
  },
  "issues": {
    "overlap_topic_patterns": {
      "title": "Overlapping topic patterns",
      "description": "This Telegraf MQTT entry's topic pattern \"{own_topic}\" overlaps with another entry \"{other_entry_title}\" whose pattern is \"{other_topic}\". The same MQTT messages will be processed by both entries, producing duplicate entities. Change one of the patterns so they cover distinct message streams."
    },
    "invalid_persisted_option": {
      "title": "Invalid Telegraf MQTT option(s)",
      "description": "The persisted value for {options} is invalid. The integration fell back to default values ({defaults}) so setup could complete. Open the integration's options dialog and correct the values, or remove and re-add the entry to reset the options to defaults."
    },
    "device_cap_reached": {
      "title": "Telegraf MQTT device cap reached",
      "description": "This entry has dropped {dropped} measurements from new Telegraf hosts because the device cap ({max_devices}) was reached. New hosts under \"{configured_topic}\" are being ignored. Add another config entry with a narrower topic pattern to split the load, or consolidate hosts."
    },
    "metric_cap_reached": {
      "title": "Telegraf MQTT metric cap reached",
      "description": "This entry has dropped {dropped} new metrics because the per-device metric cap ({max_metrics_per_device}) was reached on at least one device. Metrics beyond the cap are being ignored for \"{configured_topic}\"."
    }
  },
  "exceptions": {
    "reconfigure_subscribe_failed": {
      "message": "Could not subscribe to the new MQTT topic pattern {topic}: {error}. The integration kept the previous subscription."
    },
    "mqtt_broker_unreachable": {
      "message": "Could not reach the MQTT broker to subscribe to {topic}: {error}. Home Assistant will keep retrying."
    }
  },
  "entity": {
    "sensor": {
      "cpu_package_temperature": {
        "name": "CPU Package Temperature"
      },
      "cpu_field": {
        "name": "CPU {field}"
      },
      "memory_field": {
        "name": "Memory {field}"
      },
      "disk_root_field": {
        "name": "Disk Root {field}"
      },
      "disk_field": {
        "name": "Disk {field}"
      },
      "network_field": {
        "name": "{interface} {field}"
      },
      "sensor_field": {
        "name": "Sensor {field}"
      },
      "gpu_field": {
        "name": "GPU {field}"
      },
      "battery_field": {
        "name": "Battery {field}"
      },
      "generic_field": {
        "name": "{field}"
      }
    },
    "binary_sensor": {
      "cpu_package_temperature": {
        "name": "CPU Package Temperature"
      },
      "cpu_field": {
        "name": "CPU {field}"
      },
      "memory_field": {
        "name": "Memory {field}"
      },
      "disk_root_field": {
        "name": "Disk Root {field}"
      },
      "disk_field": {
        "name": "Disk {field}"
      },
      "network_field": {
        "name": "{interface} {field}"
      },
      "sensor_field": {
        "name": "Sensor {field}"
      },
      "gpu_field": {
        "name": "GPU {field}"
      },
      "battery_field": {
        "name": "Battery {field}"
      },
      "generic_field": {
        "name": "{field}"
      }
    }
  }
}
